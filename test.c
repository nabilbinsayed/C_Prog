#include <stdio.h>
int main(void)
{
  printf("Here's to a new beginning, cheers!\n"); 
  printf("Again, just testing out the \"git push instead of git push origin main thingy\"\n"); 

  printf("From a new PC!\n Let's test this origin main thing again!\n"); 
  
  return 0; 
}