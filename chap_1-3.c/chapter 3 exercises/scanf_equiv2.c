// Name:
// Date:
// Course/book:
// Section:
// Purpose:
// Note/Highlight:

#include <stdio.h>
int main(void)

{
  int a, b, c;
  int x, y, z;

  printf("input 3 integers: ");

  scanf("%d-%d-%d", &a, &b, &c);
  printf("%d, %d, %d\n", a, b, c);

  scanf("%d -%d -%d", &x, &y, &z);
  printf("%d, %d, %d\n", x, y, z);

  return 0;
}